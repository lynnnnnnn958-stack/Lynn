"""Page modules.

Each page should expose one public render function, for example:

``render_risk_page()``

The active monolith still uses ``tab_risk_portfolio()`` and friends. Migrate one
page at a time only after browser and compile checks pass.
"""

